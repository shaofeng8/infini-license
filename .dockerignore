# 依赖与构建产物：镜像里会重新装、重新编。带宿主机的进去不只是慢，
# node_modules 里有按平台编译的二进制（mysql2 之类），Windows/macOS 上装的
# 拷进 alpine 会在运行时才炸，而那时已经很难联想到是这一步的问题
node_modules
**/node_modules
dist
**/dist
build
**/build
*.tsbuildinfo

# 真实环境变量绝不进镜像。生产配置一律由 compose 的 environment/env_file
# 在**运行时**注入 —— 打进镜像等于把密钥焊死在制品里，
# 而镜像会被推到仓库、被人 docker cp 出来
.env
.env.*
!.env.example
!**/.env.example

# 版本库与编辑器
.git
.gitignore
.vscode
.idea

# 文档与设计稿：跟运行无关，只会让构建上下文变大
docs
*.md
!README.md

# 测试与本地脚本产物
coverage
**/coverage
*.log
**/*.log
.cache

# 本地授权状态与凭证。开发时可能在仓库里落过 license.key，
# 绝不能跟着镜像跑出去
license
*.key
!packages/server/src/**/*.key
